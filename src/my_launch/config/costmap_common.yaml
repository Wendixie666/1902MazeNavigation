transform_tolerance: 0.5
robot_radius: 0.25
obstacle_range: 3.0
raytrace_range: 3.5
observation_sources: scan

scan:
  data_type: LaserScan
  topic: /scan
  marking: true
  clearing: true
  inf_is_valid: true

inflation_radius: 0.30
cost_scaling_factor: 3.0
